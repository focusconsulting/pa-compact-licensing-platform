API_PORT='8000'
DB_HOST='localhost'
DB_PORT='5432'
DB_NAME='licensing'
DB_USER='licensing'
DB_PASSWORD='secret123'
REDIS_URL='redis://localhost:6379'

# can be 'DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL' see config.py
LOG_LEVEL='INFO'

# can be 'LOCAL_DEV', 'DEV', 'STAGING', 'PROD' see config.py
ENVIRONMENT='LOCAL_DEV'

# AWS Cognito — obtain from Terraform outputs or AWS Console
COGNITO_USER_POOL_ID='us-east-1_xxxxxxxxx'
COGNITO_REGION='us-east-1'
COGNITO_CLIENT_ID='xxxxxxxxxxxxxxxxxxxxxxxxxx'

# Comma-separated list of allowed CORS origins
# TODO will need to populate this in terraform after PR 26
CORS_ORIGINS='["http://localhost:3000"]'
